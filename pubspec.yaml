name: servifast_app
description: "Aplicativo ServiFast"

publish_to: 'none'

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  cached_network_image: ^3.4.1
  cloud_firestore: ^6.3.0
  confetti: ^0.8.0
  firebase_auth: ^6.4.0
  firebase_core: ^4.7.0
  firebase_messaging: ^16.2.0
  firebase_storage: ^13.3.0
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.3.1
  go_router: ^17.2.2
  google_fonts: ^6.1.0
  image_picker: ^1.2.1
  intl: ^0.20.2
  mask_text_input_formatter: ^2.9.0
  shimmer: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
